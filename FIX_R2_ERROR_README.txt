SUGO FINAL FIX

This build uses Cloudflare KV instead of R2 and deploys to the existing Worker named sugo, where the AI secrets are already configured. No AI key entry is requested.

Run START_CLOUDFLARE_SETUP.bat and wait for SUCCESS.
